import { Entity, Schemas, engine, SyncComponents, Engine } from '../../../packages/@dcl/ecs'
import { addSyncTransport } from '../../../packages/@dcl/sdk/network'

describe('Network Parenting', () => {
  const engineA = Engine()
  const engineB = Engine()
})
